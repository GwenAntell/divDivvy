# divvy 0.1.1 (development version)

## Bug fixes

* Patched bug in `sdsumry()` from change in names of `iNEXT` returned objects

## Improvements

* Added a `NEWS.md` file to track changes to the package.
